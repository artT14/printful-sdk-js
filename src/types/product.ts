export type SyncProduct = {
    external_id?: string,
    name: string,
    thumbnail: string,
    is_ignored?: boolean
}

export type OptionalSyncProduct = {
    external_id?: string,
    name?: string,
    thumbnail?: string,
    is_ignored?: boolean
}